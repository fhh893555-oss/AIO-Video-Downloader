buildscript {
    dependencies { classpath libs.objectbox.gradle.plugin }
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://jitpack.io' }
    }
}

plugins {
    alias(libs.plugins.android.application) apply false
}